test('Should throw error if arrray lenght is empty',()=>{
    expect()
})

test('Should throw error if the array contians strings',()=>{
    expect()
})

test('Should throw error if is not an array',()=>{
    expect()
})

test('Should throw error if variance is a negative number',()=>{
    expect()
})


